<!DOCTYPE html>
<html>
	<head>
		<title>AppName</title>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<%= csrf_meta_tags %>
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
	</head>

	<body id="<%= "page-" + params[:controller].gsub("/", "-") %>" class="gray-bg <%= "guest page-" + params[:controller].gsub("/", "-") + "-" + params[:action] %>">
		<a href="<%= auth_signin_path %>">Sign In</a> <a href="<%= auth_signup_path %>">Sign Up</a>
		<%= yield %>
	</body>	
</html>