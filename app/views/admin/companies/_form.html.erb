<% if !@state.errors.nil? && @state.info[:kind] == 'company' %>
<%= @state.errors.to_json %>
<div class="alert alert-danger alert-dismissable">
	We have found errors in a form <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
</div>
<% end %>

<div class="form-group"><label class="col-sm-2 control-label">Name</label>
	<div class="col-sm-10"><input type="text" name="company[name]" value="<%= @cform[:name] %>" class="form-control"></div>
</div>
<div class="hr-line-dashed"></div>

<% unless @state.errors.nil? %>
<script type="text/javascript">
	window.main.presentErrors(<%= raw @state.errors.to_json %>, '.company-form', 'company');
</script>
<% end %>

<div class="form-group">
    <div class="col-sm-4 col-sm-offset-2">
        <button class="btn btn-primary" type="submit">Save changes</button>
    </div>
</div>
